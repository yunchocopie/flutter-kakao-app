import 'package:flutter/cupertino.dart';

class MorePage extends StatelessWidget {
  const MorePage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("morePage"),);
  }
}